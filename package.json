{
  "name": "frontrun-demo",
  "version": "1.0.0",
  "description": "Simulate front-running attack on a quiz contract",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.22.0"
  }
}
